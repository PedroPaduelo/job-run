# Configurando Docker Compose, Postgres, com Testes de Carga - Parte Final da Rinha de Backend

- **Canal:** Fabio Akita (@Akitando)
- **URL:** https://www.youtube.com/watch?v=-yGHG3pnHLg
- **Duração:** 1:35:44
- **Fonte:** Painel "Mostrar transcrição" do próprio YouTube (transcrição automática)

> Observação: transcrição automática — pode conter `[ __ ]` (palavrão censurado),
> `[música]` e erros de reconhecimento de fala. (Neste vídeo o YouTube serviu a
> faixa de legendas em inglês.)

---

## Transcrição (com timestamps)

| Tempo | Texto |
|-------|-------|
| 0:00 | Hey everyone, Fabio Akita. Before today's topic, just confirming — yes, the last video was an experiment |
| 0:07 | in making an episode entirely generated by AI. Most of you caught it, but plenty took forever or didn't notice at all. |
| 0:13 | Don't worry, it was a one-time thing just to see how it'd turn out. It is not even slightly practical to make videos with AI tools like HeyGen. |
| 0:21 | I explained the process in the video description, which you all always skip. So go read it. Today's topic, finally, is the promised Part 2 of Rinha de Backend. |
| 0:31 | Yes, the "16 Languages in 16 Days" video dropped in September and I promised a |
| 0:36 | Part 2. It's December, almost a new year, and only now am I putting it out. But that's how this channel works: I felt like talking about other stuff first. |
| 0:46 | The point of this video is to fill in some tips and information that can help with your studies. |
| 0:52 | In particular, some of the techniques that separate a tutorial toy from a real project. |
| 0:59 | So, here we go ... (...) One reason it took me so long to make this episode is because of these two |
| 1:08 | items I'd put down to cover here: Solving the Rinha in 5 minutes and What is a web framework? |
| 1:14 | These two items would be a full 2-hour video and honestly, I don't have the patience for that subject because there's already plenty of documentation online. |
| 1:23 | Open the website of any web framework, and that'd be the first tutorial on how to use it. I'd just be repeating what's already out there. |
| 1:30 | Instead, I'll just summarize a few of the main points. Solving the Rinha in 5 minutes. |
| 1:35 | Yes, I could do a speed run and edit a video showing step by step how to build a version of the APIs. |
| 1:42 | I think it's pointless. A code speed run just to prove I can do it really doesn't motivate me. Obviously I know how to do it. |
| 1:48 | Unnecessary flex. Frankly, the 16 languages in 16 days video was already flex enough. |
| 1:55 | If you're a beginner and you're struggling to solve this exercise, obviously you still need to study more and practice more. |
| 2:02 | What do I always say? There is no single way to solve a coding problem. There's no single tutorial or course walkthrough that can cover every permutation. |
| 2:12 | What you have to do? Open the event's GitHub and go into the participants directory. |
| 2:18 | That's why I found this event interesting: the sheer number of different versions the |
| 2:23 | participants published to solve the same problem. Let me give you an example. I cloned the main Rinha repo to my machine. |
| 2:30 | Grab the link in the description below. Going into the participants directory, we have 94 versions. |
| 2:36 | I went over most of them in the other video, watch that afterwards. But let's say you're studying PHP and want to see how this problem is solved in PHP. |
| 2:44 | From the participants directory, just open a terminal and type `grep "php" */README.*` |
| 2:50 | and look at that — 4 subdirectories show up, two from Lauro Appelt and two from some guy called Predator. |
| 2:58 | In the previous video I explained one of Lauro's versions, built in Hyperf with Swoole. |
| 3:03 | Let's open the README of one of the directories and look — we've got the repo link with Lauro's PHP code. |
| 3:09 | We can clone it anywhere else and poke around to see how he implemented his version. |
| 3:15 | The most interesting part of this event is that one of the requirements was for every participant to create Dockerfiles and Docker Compose for everything. |
| 3:22 | So it's super easy to run, no need to guess how to set up the environment for each |
| 3:28 | project. These days I consider it good manners, good etiquette, that every project, no matter |
| 3:34 | how small or how big, should always have a Dockerfile and a docker-compose. |
| 3:40 | The Dockerfile shows how to run your application and the docker compose shows |
| 3:45 | how to bring up that project's dependencies, like databases. Let's look at Lauro's project Dockerfile. |
| 3:51 | First, it starts by pulling a pre-built image prepared by the Hyperf framework team, which already |
| 3:56 | has everything you'll need. Then it does some timezone configuration. Most images boot with UTC — Greenwich time, timezone zero — as the default. |
| 4:06 | To get the correct times for your geographic region, you need to set something like "America/New_York", always. |
| 4:12 | Nobody's going to guess it for you. This next big block here is optional and I didn't test it enough to know whether |
| 4:18 | it was necessary or not to improve benchmark times. All it's doing is creating a config variables file, to tweak |
| 4:25 | settings like max upload size — which is pointless for this project — or memory limit, which might or might not be useful, I didn't measure to find out. |
| 4:34 | Don't go copy-pasting this thinking it'll automatically improve something. Go to the docs and understand each item in here. |
| 4:42 | Then run one version without it, and another with it, running the same benchmark, and |
| 4:47 | compare whether it makes any difference. The fact that things irrelevant to this challenge — like max upload size — are in here |
| 4:54 | tells me Lauro copy-pasted the example Dockerfile from the Hyperf image site and dropped it in. |
| 4:59 | To wrap up, we add Postgres drivers for PHP, change the working directory |
| 5:04 | to /app, which is where we'll copy the project files next. What I found odd is there's no final ENTRYPOINT command specifying how to |
| 5:13 | run the application, and I also noticed "composer install" is missing to pull the libraries. |
| 5:19 | That's a quirk of Lauro's version — file that away, I'll explain shortly why it's missing. |
| 5:25 | Now let's look at the docker-compose.yaml. Docker Compose is the description of a mini-infrastructure. |
| 5:30 | Instead of installing, configuring, and spinning up Redis, Postgres, Nginx all by hand, we can automate |
| 5:37 | everything with a single command. It's much easier for a new developer joining your project to understand how |
| 5:43 | things work when there's already a ready-made docker compose. It works as living documentation for the project's infrastructure. |
| 5:49 | Good practice — every real project should have a working, configured docker compose. |
| 5:56 | Never written a docker compose in your life? How do you start? Simple — just grab any of the 90-something examples from this event as |
| 6:03 | a base. Let's open Lauro's and go from the bottom up. Right away, we can see this project depends on Redis being up. |
| 6:10 | That "redis" name on the first line can be whatever you want. By convention we use the name of what we're running, but it could be "cache_server" or something |
| 6:18 | like that. The only truly required line is the image name. Docker, by default, pulls everything from dockerio.com. |
| 6:26 | If you want to pull from somewhere else, you need the full URL, otherwise the name and version |
| 6:31 | of the image is enough. Here it says "latest", which is the most recent version. I don't like using latest because if tomorrow the version changes and your app wasn't updated |
| 6:40 | and there are breaking changes, your developers are going to start hitting problems. |
| 6:45 | If they're very junior, they won't connect the dots that Redis changed versions and that's why things broke out of nowhere. |
| 6:51 | Better to pin the version, like 7.2.3-alpine3.19, meaning Redis version 7.2.3 running |
| 7:01 | on top of Alpine Linux version 3.19. The other options are specific to the Rinha, in particular the resource limits in the |
| 7:09 | deploy section. That was one of the criteria, which I thought was clever. We had at most 1.5 CPUs and 3 gigabytes of RAM to use. |
| 7:17 | At first glance, that sounds like very little. It's not easy to picture how those resources fit across each component. |
| 7:23 | If you're a beginner with a weak machine, you've already had to deal with this: how do you squeeze the most out of a dual-core with 2 gigabytes of RAM? |
| 7:32 | Every developer who got good started out having to run heavy programs on weak machines. |
| 7:38 | If you're a beginner who got lucky and started out working on brand-new, beefy machines |
| 7:43 | from day one — anything above a quad-core, above 8 gigabytes of RAM, like a MacBook — and never had to wrestle with weak hardware, watch out. |
| 7:52 | You're going to end up complacent, lazy. You'll think the whole world is like your bubble: CPU to spare, RAM to spare, |
| 8:00 | and you'll never appreciate the value of optimizing things. Why optimize? If there's always more than enough? |
| 8:05 | When things get hard, you'll be the kind who folds easy. That's why I keep saying that in my "Learning to Learn" theory, you have to have the Pain of |
| 8:14 | Learning. It doesn't matter if you fall, what matters is how you get back up. It doesn't matter how many times you fall — what matters is that every time, you get up faster. |
| 8:22 | Whoever never felt pain never learned a thing. And in this Rinha, the equivalent of that is the resource constraint — otherwise, obviously, the participant |
| 8:29 | with a machine like mine — 64 gigs of RAM, Ryzen 9 with 32 threads — would easily |
| 8:35 | generate benchmark numbers way better than everyone else. But with this limit, the participant with a quad-core and 4 gigs of RAM could beat |
| 8:44 | my machine, if they knew how to better manage the limited resources. And this resource-limiting in Docker wasn't built for competitions. |
| 8:52 | It was built for when you genuinely need to run two processes on the same machine, |
| 8:58 | and in case one of them is too greedy and hogs too many resources, it doesn't leave the other process |
| 9:03 | starving. If the server has 4 gigs of RAM, I spin up two programs, each in its own Docker container. |
| 9:10 | Say the first one decides to allocate all available memory, even though it's not going to use it. Then when the second container tries to run, it'll crash from lack of resources. |
| 9:18 | Do processes exist that allocate more memory than they should? Yes — Node.js, for example, has a cluster feature, which I mentioned in the previous episode. |
| 9:26 | What do a lot of people do? They Google it and just copy a snippet similar to this from some Stack Overflow |
| 9:33 | post. This script runs "os.cpus.length" and sees your machine has 4 cores — 4 CPUs — |
| 9:40 | so it forks the application 4 times. Each fork might eat, say, half a gigabyte. So 4 forks is 2 gigabytes. |
| 9:47 | Since your machine has 8 gigabytes, you don't think twice about it. Everything works just fine. Then you spin up a server on some cloud provider with 8 gigs of RAM. |
| 9:56 | But you go for a beefy CPU — 16 cores. Your cluster script sees 16 CPUs and spins up 16 forks. |
| 10:05 | Each fork eating half a gig of RAM. Which means it'll eat all 8 gigs at once. |
| 10:11 | And you don't have 8 gigs to spare — your OS and other services eat RAM too. |
| 10:16 | Some of those forks are going to crash right at boot. The right fix is to patch that script and replace the os.cpus call with an environment |
| 10:26 | variable instead. That way we can declare that variable, like CLUSTER_WORKERS, in the docker compose yaml, |
| 10:32 | and have control over how many processes actually spin up. As a developer you always need to have control over exactly what's going to run |
| 10:41 | and how many resources it'll use. A great way to drill this is by nailing your docker compose scripts. |
| 10:48 | Moving on — after redis we have nginx. That was another requirement in the Rinha instructions: have a reverse proxy to serve |
| 10:55 | as a load balancer across at least two instances of your application. When a student follows any web tutorial, they only understand that there's a single |
| 11:05 | process — the application server. It spins up on a port like 4000, you put localhost:4000 in the browser, and that's it. |
| 11:13 | That's level zero in the learning curve. The level just above that ultra-basic one, you need to learn that in real projects, we spin up |
| 11:19 | multiple processes in parallel — either on the same machine or across multiple machines. |
| 11:24 | If it's on the same machine, one runs on port 3000, another on 3001, another on 3002. |
| 11:30 | In either case, we always put a load balancer in front, in the form of a reverse proxy. |
| 11:36 | I cover this in the episode "Making Your Web App Faster." Some more experienced folks might criticize the choice of NGINX, since there are other programs |
| 11:45 | more specialized for this, like HAProxy, or more modern ones like Envoy or Caddy. |
| 11:51 | But it's absolutely not a bad choice, especially because it's one of the best-documented, easy to use, and for this Rinha, it's more than good enough. |
| 11:58 | In this docker compose section we have two important things — the first is the image. In this case we're using the Alpine version, which is one of the lightest. |
| 12:06 | More important is the volume mapping. We need to map the config file that sits in our project on the outside, |
| 12:13 | so the container can access it when it boots. Remember — a container is just a way of lying to the process running inside it, so |
| 12:21 | it thinks it's alone, on an empty machine. It can't see any files from outside unless we explicitly declare them inside, like |
| 12:29 | this. What's in that config file? Let's take a look. If you've never seen it, it looks complicated, but let's take it piece by piece. |
| 12:37 | This first part at the top has to do with the service's scalability. Nginx runs on a master-and-workers architecture. |
| 12:44 | Setting "worker_processes" to "auto" means it'll evaluate how many CPUs |
| 12:49 | your machine has and spin up 1.5x that. So if you have 4 CPUs, it'll spin up 6 workers. |
| 12:56 | Why not 1 worker per CPU? First, a CPU isn't exclusive to 1 process. |
| 13:03 | What controls which process runs on which CPU during a given time window is the kernel |
| 13:09 | scheduler. If your process is lightweight and needs very little time to do its thing, there's no reason to lock up an entire CPU just for it. |
| 13:16 | It can share with other processes. Watch my episodes on concurrency and parallelism later to understand how schedulers |
| 13:23 | work. Next we have worker_connections set to 4096. That indicates how many simultaneous connections each worker will try to handle. |
| 13:32 | Don't confuse simultaneous connections with parallel processing. If 4096 web requests actually come in at exactly the same instant, this parameter |
| 13:41 | just means the worker will accept all 4096 connections, but a good chunk of them will be |
| 13:47 | waiting in a queue until they get served. If it takes longer than a configured threshold, it can reject and return a |
| 13:55 | timeout error. As the name says — time expired error. In the first Rinha episode I explained how a lot of people were just tweaking |
| 14:02 | this number by guesswork. Only Leandro, who built the Ruby version, understood that the trick wasn't to increase |
| 14:09 | this number but to decrease it. I explain the reasoning in the other video, so make sure to watch it afterwards. |
| 14:14 | Nginx is just the order-taker — remember the fast food cashier analogy? |
| 14:20 | It takes the orders, but the ones who actually execute them — the cooks — are these servers declared in this upstream section. |
| 14:26 | In this case pointing to localhost, ports 9501 and 9502. If you've worked with docker compose before, this might look a little strange. |
| 14:36 | Normally, to spin up nginx via docker compose, I'd have a configuration more or less like this: I'd declare two web services, the instances of my application, |
| 14:45 | as app1 and app2. Both running on port 8080. Then, in the nginx config, this upstream section would point to app1 on port |
| 14:55 | 8080 and app2 also on port 8080. But how can you have two services running on the same port? |
| 15:01 | No conflict? There isn't one, because in practice each container is like a separate external server |
| 15:08 | on its own. And your application is running on an external server — just a virtual one. |
| 15:13 | So each server has a free port 8080. Each instance can bind to port 8080 of its own respective "virtual server." |
| 15:22 | It happens that nginx and these virtual servers communicate on the same network — also virtual — |
| 15:28 | so nginx can load-balance between these two services. In the previous episode I explained the discovery some of us made during our |
| 15:38 | experiments — Vinicius Ferraz discovered that if we use the virtual bridge network mode, |
| 15:44 | which is the default, it introduced a heavy bottleneck and the benchmark results |
| 15:49 | were way off. But if we set the network mode to host — meaning no network virtualization — then the times |
| 15:56 | finally come out right. You may have noticed that in the redis config I showed earlier, or this nginx one, |
| 16:03 | both already have the network_mode option set to host, indicating they should bind |
| 16:09 | the container to the real network ports of the native operating system. In this mode we can no longer think of having "separate virtual servers" — instead, we're actually spinning |
| 16:18 | everything up on the same machine, on the real network. Therefore, we need to worry about port conflicts. |
| 16:25 | That's why in nginx's upstream, pointing to two servers on localhost, each of them |
| 16:30 | needs to be on a different port — 9501 and 9502. It's very important to understand what bridge network mode is and the difference with |
| 16:40 | host network mode. Study up if you didn't know this. But since we're on the application server. |
| 16:45 | Notice that in the nginx config there's this depends_on section. It means: only bring up the nginx service once it detects that services api-01 and |
| 16:55 | api-02 are up. So let's jump to the top of the docker compose file to see how this application |
| 17:01 | is configured. We need to configure the same application twice — as api-01 and then |
| 17:07 | as api-02 — and since we're using host network mode, we need to declare different ports |
| 17:12 | for each one, which we normally do via environment variables, like this HTTP_PORT |
| 17:17 | right at the top. The easiest way is to configure this api-01 service config block and then |
| 17:23 | copy and paste it below to be api-02, just changing HTTP_PORT to 9502. |
| 17:29 | There are other ways to do this. As an example, let's look at Lucas Poole's Node.js project docker compose, because |
| 17:36 | he's still using the default network mode via virtual bridge, so he didn't need to worry |
| 17:41 | about configuring different ports, he just needed different hostnames. Let's take a look. |
| 17:46 | He declared the service named "app1" and then attached a label with this ampersand, |
| 17:52 | calling it "app." Scrolling to the next service in the same file, notice — we have app2. And the next line is like an "include" or "import", telling it to copy everything that was |
| 18:04 | in app1. The only thing that changes is the hostname. Get it? That's how we can reuse the same configuration across different services. |
| 18:13 | This is a feature of YAML config files, not something specific to docker compose. |
| 18:19 | Any other program that uses YAML can do the same thing. As I said before, Lucas's project isn't using host network mode — it's using |
| 18:27 | bridge mode, so in app2's config, he keeps the same port number. If it were in host mode, he'd need to declare at least a different port number |
| 18:36 | to avoid conflict on localhost. If you've never seen this before, I know it can be confusing — but remember this difference between |
| 18:42 | virtual bridge network and native host network. The most important part in configuring our API application is this |
| 18:51 | "build" parameter. Here it's more verbose than it needs to be. Writing just "." would have been enough, but Lucas preferred to explicitly declare the context as "dot" |
| 18:58 | and the file as "Dockerfile" itself, which is already the default. This is where it'll use the Dockerfile we saw earlier to build the image |
| 19:06 | for this container. All the other services, we point them to pre-built images from the Docker Hub |
| 19:12 | service. Those images were all built with some Dockerfile too. The difference is our application hasn't been published to Docker Hub — we're building |
| 19:21 | it right here, so we point directly to the Dockerfile. In the future, once the application is done, we can upload the final generated image |
| 19:28 | and start pointing to it via the image parameter instead of build. In Lucas's file, that option is even commented out, because to participate in the Rinha, you needed |
| 19:37 | to have published a real image. Lauro's Dockerfile, which I showed earlier, uses the Hyperf repository image as its base |
| 19:44 | and also includes things like the Postgres PDO driver. All these elements can be configured via environment variables, which is what we're |
| 19:51 | declaring in the "environment" section, like HTTP_PORT — which tells the Hyperf server which port to spin up on — or DB_HOST and REDIS_HOST, which declare the server name that'll host |
| 20:02 | the database and cache. In this case, in host network mode, it'll all be localhost. |
| 20:07 | In Lauro's project, when we did the performance optimizations I explained in the other episode, |
| 20:12 | he made some adjustments afterwards and preferred not to leave everything 100% automated. Remember I mentioned that his Dockerfile was missing the ENTRYPOINT command, which defines |
| 20:22 | which executable to start in the container? To know what to do, you have to read his README. When you bring up Docker Compose, the containers will be up, but internally what's running |
| 20:31 | is this "sleep infinity" command — a trick just to keep the container alive and not die. |
| 20:37 | The reason for that is he didn't add the next steps in the Dockerfile — not sure |
| 20:42 | why — but the README asks you to manually run, via "docker exec", the "composer install" command |
| 20:49 | to install the dependencies. Since he maps the entire directory as "/app" inside the container, if I'm not mistaken, |
| 20:55 | "composer install" will download and write the dependencies to the "/app/vendor" subdirectory. |
| 21:01 | That's why it doesn't matter which of the two containers you run it in — the other one will see it automatically, since they share the same volume on disk. |
| 21:08 | Only after doing that can we, via docker exec again, run Hyperf in each container, |
| 21:14 | and it'll start right up. During development, dependencies like node libraries — or in this case, composer ones — |
| 21:20 | we don't want to bake them into the image because they take up too much space and change constantly. Rebuilding the image every time would be a pain. |
| 21:27 | Better to map a volume inside the container and install there. Only when you're generating the final image to publish — that's when you'd want to switch |
| 21:36 | to baking the dependencies into the image. Otherwise whoever pulls it later will need to do that step manually, which isn't ideal. |
| 21:42 | Once all that's done, from the terminal, just bring everything up with "docker compose up." That'll pull any images you don't have yet, build the application one, and spin |
| 21:51 | everything up cleanly, in the right order, thanks to the depends_on all services declare. |
| 21:56 | Look at that — the first time it takes a little bit to come up. I'll fast-forward a little and... done. |
| 22:02 | As I explained before, just for Lauro's project we need to run composer install manually — fast-forward a bit more and... done. |
| 22:11 | Finally, we can bring up Hyperf in each of the containers. To test that everything worked, we can hit the people-count API endpoint, |
| 22:20 | which is used by the benchmark script to show how many people were inserted during the load test. |
| 22:26 | Just call it from the terminal with curl hitting the local count-people endpoint on port nine nine nine nine. And... it blew up. |
| 22:32 | What happened? If you're running on some Linux distro, it would've come back clean. |
| 22:38 | But if you're on Mac or Windows — like I am, via WSL 2 — that host network mode config |
| 22:44 | is not going to work. Why? If you don't understand how a container works under the hood, make sure to watch the episode |
| 22:52 | on containers. In short, Docker needs the Linux kernel to work. |
| 22:57 | Docker isn't a virtual machine — it's an infrastructure that asks the kernel to lie |
| 23:02 | to processes, making them believe they're running alone on the machine. On Mac, the problem is that Macs don't run a Linux kernel. |
| 23:10 | Even though they run programs that look like Linux ones, they're all actually recompiled |
| 23:16 | to be compatible with the Darwin version of BSD UNIX. Macs are UNIX, not Linux. |
| 23:22 | I told that whole story in the episode on the differences between Mac, Windows, and Linux. Linux programs don't run natively on Macs — they all need to be at least recompiled. |
| 23:32 | Mac and Linux are binary-incompatible. Windows, obviously, is also not a Linux distro. |
| 23:39 | The Windows kernel is fundamentally different from Linux and Mac. But these days there's WSL 2, which is basically an ultra-light Linux virtual machine running |
| 23:49 | on top of Windows using the Hyper-V hypervisor infrastructure. That's the only solution for Mac and Windows: run Linux in a virtual machine and, inside |
| 23:59 | it, run Docker. For both Mac and Windows, one of the most popular options is downloading the |
| 24:05 | Docker Desktop package, which installs a VM for Docker and a GUI — |
| 24:11 | optional — to make it easier to manage the various Docker elements like virtual networks, |
| 24:17 | images, and containers. And here's the problem — why localhost:9999 doesn't work in host network mode. |
| 24:23 | According to the docs themselves: the host network mode driver only works on Linux — running |
| 24:29 | in Docker Desktop's VM, the container won't get any IP address and port publishing will be ignored, making the container unreachable. |
| 24:38 | Does that mean Docker Desktop on Windows and Mac is garbage, then? Let's recap — why were we using host network mode? |
| 24:45 | Because in the load testing scenario, the default mode creates a bottleneck, so we switched to |
| 24:51 | host mode. That's only for the load testing situation. Day-to-day, while you're coding, under normal circumstances, you won't notice any |
| 25:00 | difference. In that case, you don't need to use host mode — just leave everything at default. |
| 25:05 | In virtual bridge mode, Docker on Mac and Windows will work just fine. With WSL 2, you can install Docker or Podman inside WSL instead of using a separate |
| 25:16 | VM like Docker Desktop does. Speaking of Docker Desktop — quick tangent — if you really want a GUI |
| 25:23 | to manage images, containers, and everything else, try another option: Portainer, which is itself an application that runs in a Docker container, so it's trivial to install, |
| 25:32 | and it gives you a web admin interface that's way more powerful than Docker Desktop. But it's also cool to learn to stick to the command line and experiment with things like Lazy |
| 25:42 | Docker. You can do everything with just the "docker" command, but sometimes we forget how to do things we don't use every day — like "wait, how do I delete images that aren't |
| 25:50 | being used again?" — and that's where Portainer and Lazy Docker can help. Go poke around. Actually, using port NAT — the default option — is way simpler. |
| 26:00 | Let's take Lucas's nginx file, since he didn't go with host network mode. |
| 26:06 | Look at the upstream server declaration. Instead of localhost with two different ports, we have two different hostnames here |
| 26:13 | using the same ports. It's like declaring two separate servers, each with a copy of the application |
| 26:20 | on port 8080. In docker compose, we declare app1, expose port 8080, tag it with the ampersand |
| 26:28 | "app", and down below declare app2 using the same configuration, just overriding the hostname. |
| 26:33 | Everything else stays the same. We don't have to worry about port conflicts because it's as if they're servers |
| 26:39 | on different machines. That's the advantage of port NAT and why it's the default mode. |
| 26:44 | It's much more intuitive. If that wasn't clear, watch my containers episode again and the networking |
| 26:50 | series. Lauro's PHP version is one of the trickier ones to bring up, because of those |
| 26:56 | extra steps in the README that he chose not to automate. To continue the explanation, we'll use Lucas's Node project from here on out. |
| 27:04 | His is simpler — just run "docker compose up," wait for everything to pull, and done. |
| 27:09 | Even on Docker for Windows or Mac it shouldn't have issues because his version uses standard port NAT, so everything should work plug-and-play — which is the ideal for a docker |
| 27:19 | compose. Before I forget — if you're not used to docker compose, always remember that if you don't start the "up" command with the "-d" daemon flag, it'll keep hogging your |
| 27:28 | terminal. Ctrl-C will bring everything down. And make sure the containers are actually stopped by running "docker compose down." |
| 27:36 | If you modify your code and bring it back up with "up" but feel like your changes aren't showing, |
| 27:41 | you can force the app image rebuild with "docker compose up --build" or "docker |
| 27:47 | compose up --force-build." From another terminal, you can see the running containers with "docker ps." |
| 27:55 | Look at how cleanly it shows — from there you can open a shell into any of them using "docker exec -it." |
| 28:01 | While working on a project, you can just keep tearing it down with "down" and bringing it back up with "up." |
| 28:07 | But when you're done with a project and won't be touching it anymore, it's worth doing one final teardown with "docker compose down --rmi all." |
| 28:15 | That option will delete the containers and the images. Docker images eat up a lot of disk space, and if you keep forgetting to clean up after projects |
| 28:23 | you're done with, you'll be wasting space. Look up later how to clean containers and images you're no longer using. |
| 28:31 | Back to it — time to dig into this load testing business. Understand this: who cares what language you picked, what framework you use, the mountains of JavaScript |
| 28:42 | you crammed into the front-end. Svelte, Tailwind — none of that matters. The only thing that matters is that between your browser and your application, |
| 28:51 | text-formatted messages travel back and forth, obeying the HTTP protocol. |
| 28:57 | Messages split into headers and a body. The test I suggested earlier — that curl call against the local count-people endpoint — is an example of that. |
| 29:07 | Curl and Wget are command-line browsers. Simple browsers with no front-end, so they don't need to parse HTML, |
| 29:16 | CSS, or JavaScript. They just print what they receive. I debated whether to spend time explaining curl — how to send and receive HTTP messages. |
| 29:24 | But that'd be an unnecessary tangent. Every web developer has to know how to send and receive HTTP messages manually — |
| 29:33 | whether via curl on the command line or via web developer tools in any browser. Without knowing this, I'm sorry, you haven't even started understanding the web yet. |
| 29:42 | Take several steps back, study more, then come back. I'm being blunt about this because it's real: HTTP is simple — the main headers |
| 29:51 | you can know by heart, message encodings like URL encode are simple. |
| 29:56 | And if you don't understand how this message exchange works, everything from here on out won't make any sense. |
| 30:02 | Without knowing this you don't even understand what an API is. Without knowing this you can't even begin to talk about trivial security issues |
| 30:09 | like request forgery, or performance optimizations like conditional GET requests. |
| 30:15 | 80% of the most interesting and most important stuff — you'll never have access to it. So stop here and go study first. |
| 30:22 | With all that said, let's jump straight to Gatling — the stress test tool, or load test tool, that the Rinha chose. |
| 30:28 | They even did a small sponsorship — I think with some prizes for the winners. It's a tool that accepts scripts written in Scala or Kotlin. |
| 30:36 | First thing is having Java installed on your machine. Heads up — Java 21 came out not long ago, and depending on your distro, like Arch, |
| 30:44 | which installs the latest of everything, you're going to have problems. Manually pick OpenJDK 17 or older versions like version 11. |
| 30:52 | How do I know I've got the right Java? Come on — open a terminal and type 'java -version'. |
| 30:58 | If you have more than one JDK installed — on Arch, there's the 'archlinux-java set java-17-openjdk' script to force it to use version 17, for example. |
| 31:09 | Check the documentation for your distro's JDK package. On Windows or Mac, just download the version 17 installer directly and you're done. |
| 31:18 | After that you need to download Gatling itself. Just go to the site and download the zip. Then unzip it wherever you want. |
| 31:24 | I personally prefer to unzip to "/opt/gatling" but you can unzip to your |
| 31:30 | home directory. Anyway — the important thing is to go into the Rinha repo, into the directory called "stress-test." |
| 31:36 | In there you'll find the main script, "run-test.sh." There's also a PowerShell version, but honestly I didn't bother with that. |
| 31:42 | Edit that script and make sure the GATLING_BIN_DIR variable is pointing to the |
| 31:48 | bin directory that came inside the zip you downloaded. Look at the line below — that's how it runs Gatling, passing in the simulation script |
| 31:55 | which is that RinhaBackendSimulation, which we'll look at shortly. Next it declares the main working directories. |
| 32:02 | Everything lives inside "stress-test/user-files." In resources we have spreadsheets with test data. |
| 32:08 | Let's take a look. The "pessoas-payloads.tsv" file has fake people data that it'll send to the |
| 32:15 | register-people API. As you can see, each line is a JSON, and the data is completely random. |
| 32:21 | You think everybody sends clean, pretty data to your application? On the contrary — with the amount of bots out there, your application is receiving data |
| 32:28 | just like this all the time. Never assume your application will only be used by smart people who know how to read |
| 32:33 | and write properly. On the contrary — assume most usage will come from broken bots or clueless users |
| 32:40 | who can barely spell their own name and will fill everything out wrong. Your application needs to be prepared for the worst inputs, always. |
| 32:48 | This is a good example of an attack to see if invalid things manage to slip through your validations — or more commonly, whether you forgot to implement some validations. |
| 32:57 | The second file is "termos-busca.tsv," which will be used by the search-by-terms API. |
| 33:03 | It's another list of random nonsense. Same idea — stress your APIs and see how they behave. |
| 33:09 | You need validations as specified in the Rinha instructions. For a real project, it's good to have load tests with completely random data |
| 33:16 | like these, but it's also good to have data a bit closer to what average users would send, to make it easier to analyze afterwards. |
| 33:24 | Creating test data is, in itself, an art as well. Tests with random inputs like these are called Fuzz Tests. |
| 33:31 | Fuzz is like noise, chaos — a way of saying random. If you only test with data that looks real, you're missing the opportunity |
| 33:39 | to see the limits of your validations. Finally, we have the actual load test script itself — this RinhaBackendSimulation.scala file. |
| 33:48 | Even if you've never written Scala before, writing simple scripts like this isn't hard. Any good programmer should be able to at minimum read and understand it without much difficulty. |
| 33:59 | Let's go — piece by piece. Gatling has the concept of a Simulation and our class inherits from it. |
| 34:05 | Next we start by defining where to send our load. In this case the base URL is localhost port 9999, where we configured NGINX in Docker Compose — |
| 34:14 | remember? Since our application has no handling for any headers, that's already enough. Just for fun, they put a fake user agent in here. |
| 34:22 | We continue by declaring a "create and look up people" variable which is a "scenario." I imagine this "scenario" function lives in the abstract Simulation class I'm extending. |
| 34:31 | It returns an object where we can chain multiple configuration calls. The configuration API is very readable. |
| 34:38 | Look: in the "create and maybe look up people" scenario, feed from the TSV file |
| 34:44 | "pessoas-payloads.tsv" and treat it as circular — meaning if we reach the end of the |
| 34:51 | file, restart from the first line again. For each line, execute the following block. |
| 34:56 | In this block we start by calling another simulation method, "http," which I imagine controls |
| 35:01 | an HTTP call. We configure it with a POST call to the "/pessoas" endpoint, where the body will be the payload — |
| 35:09 | which is a line from the load file we're reading. As the only header, we set "Content-type" to the mime-type "application/json." |
| 35:18 | Any self-respecting framework checks the content type. I'm not entirely sure what this "check" function does, and I didn't have the patience to go read |
| 35:24 | the docs, but it doesn't seem important right now. It must be something like: "consider it correct if the request comes back with a |
| 35:32 | status code of 201, 422, or 400." If it comes back with a crash — error 500, or page not found which is 404, or anything else, |
| 35:41 | consider it an error and report it in red. Next there's a new check, asking to save something called "http status." |
| 35:47 | We can see it'll be used in the checkIf call below. This line seems to say: execute the following block if the HTTP status code |
| 35:58 | we saved in the previous line is code 201 — the correct HTTP code when |
| 36:03 | a resource is created on the server. If that's the case, the following block should save the value returned in the |
| 36:10 | Location header, inside this session called "location." Remember this for API design: whenever a resource is created on the server, returning |
| 36:19 | code 201, it should also return a header called Location with the full URL for this |
| 36:25 | new resource. That's what we're storing. Leaving this block, the scenario pauses for 1 to 30 milliseconds. |
| 36:32 | Then, if a location session was saved, it fires a new HTTP request to that location. |
| 36:40 | If found, the server should return HTTP code 200 and the record's JSON. |
| 36:46 | If the return is 200, the simulation should consider it green — OK. If any other code comes back, consider it a red knockout. |
| 36:54 | As the comment explains, Zanfrancheschi — who designed the Rinha challenge — did things |
| 36:59 | right. For the challenge to be even remotely interesting, just building an API that saves a new person |
| 37:04 | to the database would be way too simple. Anyone who's finished at least one course tutorial should be able to do that. |
| 37:11 | But spinning up two instances of the same application and putting them behind a reverse proxy creates a somewhat more interesting situation. |
| 37:19 | Say you did some premature optimization. Let's try to be clever. When you receive the JSON via POST at /pessoas. |
| 37:26 | You hold it in memory without sending the insert to the database, to write asynchronously later, |
| 37:31 | and that way you can respond immediately with HTTP code 201. Since you generated a random UUID before sending to the database, you can generate the Location URL. |
| 37:41 | It'd be something like the local people endpoint followed by the new record's UUID. The Gatling script pauses for 1 to 30 milliseconds and then fires a GET for that URL that came back |
| 37:53 | in the Location header. If before those 30 milliseconds your application hasn't actually written to the database, |
| 37:59 | there's a chance the lookup hits the second instance. If the record didn't have time to be written, it won't be found and we'll get a 404 |
| 38:07 | not found. The simulation would log an error. So just holding the record in memory to respond faster would break that |
| 38:15 | search condition in the test — you need to be smarter than that. The Rinha's expectation — with the imposed CPU and RAM limits, the requirement to run |
| 38:24 | two or more instances, and dumping heavy load — would trick more seasoned programmers |
| 38:30 | into falling for premature optimization, adding things like caches or queues, as |
| 38:36 | I explained in the previous episode. If you do it the naive way — first write to the database, then return code |
| 38:43 | 201 with the Location URL — everything works correctly. But will the wait time of writing to the database make NGINX time out |
| 38:53 | on a lot of requests? And no — that's what we discovered in the 16 languages episode exploration: all of us |
| 39:01 | underestimated the Postgres database. It turns out that for simple cases like this — simple inserts — it's just as fast |
| 39:09 | as a cache server like Redis. Any extra optimization makes no difference. The optimization of using host network mode instead of virtual port NAT made more of a difference |
| 39:19 | than trying to optimize Postgres with some caching solution. I'll come back to that — let's just finish reading the simulation file first. |
| 39:27 | The people-creation scenario, followed by the lookup of that newly created person, |
| 39:32 | is the longest scenario. Not hard — just long. The other two scenarios are shorter. |
| 39:37 | In the "search people" scenario, it feeds from the TSV file "termos-busca.tsv," again |
| 39:43 | in circular mode, and for each line in the file, fires a GET request to the "/pessoas" endpoint |
| 39:49 | passing the variable "t=", concatenating the search parameter. I think if you don't configure it explicitly, the scenario assumes 200 responses are |
| 39:57 | OK and anything else is an error. Finally, we have the invalid people search scenario — same endpoint, just without |
| 40:04 | passing any search term. According to the Rinha rules, this should return error code 400, which is exactly |
| 40:11 | what the script will check. If it returns 400, mark it green — OK. With the 3 scenarios declared, the script now defines a block it calls "setup." |
| 40:21 | Literally configuring how the scenarios should run. In all cases, it starts by saying "begin with 2 constant users per second, for |
| 40:30 | 10 seconds," then continues with "proceed now with 5 users per second, for |
| 40:36 | 15 seconds." That's a light load — just a warm-up. Then the real load begins — look at this: "ramp" means to gradually increase, |
| 40:47 | like an actual ramp. Ramp up, starting at 6 users per second, all the way up to 600 users per |
| 40:53 | second, accelerating constantly over 3 minutes, maintaining linear growth. |
| 40:59 | Now we're talking — that'll generate heavy load all the way to the end. Get it? But that's not all — in parallel, it'll also run the search and invalid search scenarios. |
| 41:07 | Both do a small warm-up for the same 25 seconds. Then it goes straight into a ramp from 6 users per second, up to 100 users doing |
| 41:17 | valid searches per second, for the same 3 minutes. And finally, a ramp from 6 users per second up to 40 doing invalid searches per second, |
| 41:27 | for 3 minutes. In each scenario, we only configured that search is a GET to "/pessoas" or creation is a |
| 41:34 | POST to "/pessoas" — but how does it know where to send those requests? |
| 41:39 | That's what that configuration we set up at the beginning of the script is for, declaring that the base URL is "localhost" port 9999 — remember? |
| 41:46 | At the end of the setup, we plug in that configuration. And that's it! Let's see it running. |
| 41:52 | To make it clear — I have a terminal split in two, using TMUX's horizontal split. |
| 41:57 | In the top half, we're in Lucas Poole's Node project. We can call "docker compose up" and wait for all the services to come up. |
| 42:05 | In the bottom half, I'm in the "stress-test" directory of the Rinha repo, where the "run-test.sh" script lives, which we're about to run. |
| 42:13 | There we go — those 25 seconds of warm-up pass and then the fun begins. |
| 42:18 | Now we wait for the 3 minutes of the scenarios. Remember how the script configures 3 scenarios? |
| 42:24 | Here they are: Invalid Search, Valid Search, and Creation. At the top there's a summary, at the bottom a bit more detail. |
| 42:31 | The main thing: OK means requests that returned valid HTTP response codes, like 200, |
| 42:36 | 400, or 422. Anything that returned an error — code 500, timeout — I think it counts as KO, Knock Out. |
| 42:44 | Those are the lost requests. Getting back to it — after running for a while, when the load cranks up near the end, in several of the |
| 42:51 | participant projects, we'll see this error reported: java.io.IOException: Premature |
| 42:57 | Close. That's the error Vinicius Ferraz discovered how to fix by switching from network_mode bridge |
| 43:04 | to host. Look at what this message is saying: more than 9,000 lost requests from that |
| 43:09 | exception. 9,000 out of a total of just over 100,000 requests is a significant number — |
| 43:15 | almost 10% error rate. Let's open the chart that Gatling generates. In an ideal world we'd want to see only this green bar, nothing red next to it. |
| 43:24 | But we've got red. Looking at this table — out of a total of 108,000 requests, almost 99,000 |
| 43:30 | came back OK, but nearly 10,000 in red, knocked out. I'll come back to that table — let's look at the charts below. |
| 43:37 | Remember how we configured it to start the three scenarios in parallel, ramping up from 6 users to over 600 simultaneous users per second? |
| 43:45 | A ramp up? That's why the chart is shaped like a triangle. Starts at 6 users per second and goes up — ramping up, with constant acceleration, |
| 43:55 | increasing speed linearly, up to over 600 users. For over 2 minutes — more than two-thirds of the time — it grows without problems. |
| 44:03 | But then look at this final stretch. The end is full of ups and downs — this jagged pattern. |
| 44:08 | The application is clearly choking here. Lots of queued requests can't get served, or take way too long to respond, |
| 44:16 | or time out from waiting too long. That's why in the last chart we start getting several red areas. |
| 44:22 | A reminder that many of those reds are the Java IOException I mentioned earlier. But does it really make that much of a difference? |
| 44:29 | To demonstrate, we can modify Lucas's code to use host network mode. |
| 44:35 | We start by changing "index.js" which starts Node.js on port 8080 — hardcoded — and replace it |
| 44:41 | with an environment variable called "HTTP_PORT" that we can set in docker compose. |
| 44:47 | Now we add that variable in docker compose and set network_mode to host on all services, as in this snippet. |
| 44:54 | Notice how we also changed the database connection URL to be localhost. There'll be no more NAT to a virtual host called "postgres." |
| 45:03 | Docker's virtual network has an internal DNS that resolves names like "postgres" to the |
| 45:09 | virtual address, in virtual network mode. In host mode, the container binds directly to localhost, so we have to worry |
| 45:16 | about port conflicts now. The last thing — after adjusting docker compose — is adjusting the nginx config. |
| 45:23 | Again, no more internal DNS from the virtual network — everything is localhost, so we need to change the reverse proxy's upstream servers to localhost. |
| 45:32 | We spin up one app instance on port 8080 and another on 8081 to avoid conflict. Done — I didn't show the config for the other services in docker compose but you should |
| 45:40 | get the idea by now, right? It's left as an exercise to apply this change to one of the Rinha projects. I myself submitted pull requests to several of the participant projects specifically to |
| 45:50 | add this small optimization. If I could do it, so can you. Now we can bring it back up with "docker compose up" again, and if there are no error |
| 45:59 | messages, in the other terminal we can run Gatling again. I won't show the whole thing running again — let's go straight to the end. |
| 46:06 | The last thing the Gatling script does is call the "/contagem-pessoas" endpoint. |
| 46:12 | Last time it came back showing it had written about 40,000 records to the database. But now — look at the difference — it returned almost 47,000 records. |
| 46:19 | That's a considerable difference. If we go back to the results showing in the terminal, look — there's no more of that |
| 46:26 | Premature Close message from the Java IOException. Let's open the charts. I'll put the old one on the left and the new one on the right. |
| 46:34 | Same application — the only thing different on the right is that we brought up docker compose with services in host network mode. |
| 46:40 | Right off the bat, we can see the red bar is gone. Great sign. Some orange showed up. |
| 46:47 | What was red before — now it takes longer, but it goes all the way and responds. That's what orange means. |
| 46:52 | Look at the table below — before we managed a total of 108,000 requests, now we're over 115,000 requests. |
| 47:00 | More importantly: before there were nearly 10,000 knockouts total, but now it's only about 150. |
| 47:06 | Brutal difference. Zooming in here on the side, there's a concept I think everyone should understand. |
| 47:12 | Almost every beginner thinks in terms of average response time. Average is this column here toward the end: "mean." |
| 47:18 | What is average? It's tabulating how long it took to respond to each of those 115,000 requests, adding |
| 47:24 | it all up and dividing by the total number of requests. That time came out to 879 milliseconds. |
| 47:29 | Almost 1 second per request. If you had any doubt, that response time is absurdly slow. |
| 47:34 | Anything above half a second is unacceptable in any application. The right target is always under 100 or 50 milliseconds. |
| 47:43 | Look right next to it — standard deviation of 1.7 seconds. The standard deviation is bigger than the mean. |
| 47:49 | Just one standard deviation over and we're already talking over 2 seconds per request. That's a full order of magnitude. |
| 47:55 | What does this tell you about your application? Nothing meaningful. Let's understand the numbers in the earlier columns — those are more interesting. |
| 48:03 | Look here — 99th percentile. This column shows how long it took to respond to the slowest 1% of requests in this test. |
| 48:10 | That's roughly 1,150 requests. Add up their times, divide by 1,150, and that comes to almost 8 seconds. |
| 48:16 | Makes sense — they're probably the last thousand-odd requests in this load test, when we're approaching the 600 users per second we configured in the Gatling |
| 48:27 | scenario. Drop down to the 95th percentile — the slowest 5%. It falls to just over 5 seconds per request. |
| 48:35 | Go to the 75th percentile — the slowest 15% — and it drops to around 1 second per request. |
| 48:42 | See how the times fall dramatically. At the 50th percentile — the average of the slower half of requests — it's in the 2 millisecond range. |
| 48:51 | Let's look at the charts down below to get a better feel for this. Look at this first chart — see how response times per request stayed |
| 48:59 | super low, around 2 milliseconds, for almost the entire load test. |
| 49:05 | Remembering that we configured Gatling to ramp from 6 users per second up to 600 over 3 minutes. |
| 49:11 | For over 2 minutes, our setup held up great — that time barely varies, even as |
| 49:16 | the number of users increases. Then the panic begins. Watch how response times spike dramatically — jumps into the 5-second range, tries to drop, |
| 49:26 | but climbs back up to nearly 10 seconds. In the chart just below we have the number of requests per second. |
| 49:33 | Again, since we're ramping user count linearly, the request count also rises linearly — and if we had enough resources, this chart would be perfectly linear all the way |
| 49:43 | to the end. But in that same stretch where response times spike, it starts getting jagged, |
| 49:49 | until it takes a dramatic crash in the number of requests per second we can serve. The blue chart is the number of requests we could accept, while the chart below |
| 49:59 | is the number of responses our application managed to send back. Green is OK, red is knockouts. |
| 50:06 | Thanks to host network mode, see how there's barely any red — but if we look at the previous chart, see how much more red there was in that response range. |
| 50:15 | Back to the table — understand: average response time per request tells you nothing. |
| 50:21 | Average of 879 milliseconds per request. Says nothing. Now, reading the chart, we know the request that took the longest to respond took |
| 50:29 | over 9 seconds — that's this maximum column. We know that even though 5% of requests took forever — in the 5-second range — |
| 50:38 | the vast majority managed to get a response in the 2 millisecond range, which is excellent. |
| 50:43 | What caused that slowness of seconds in the 95th percentile range isn't a code problem — |
| 50:48 | it's the actual resource limits of the infrastructure. There's not much more you can do in code to shorten the time significantly. |
| 50:57 | I know because I tried — in 16 languages, remember the other video? It's easier to just add more resources — a bit more CPU, a bit more RAM. |
| 51:07 | On the other hand, if at the 50th percentile the times were in the 1-second range or higher. |
| 51:12 | Then yes — the problem would definitely be in the code, a bottleneck that would need to be fixed. |
| 51:18 | You need to run benchmarks like this on various types of projects, various types |
| 51:23 | of endpoints, to start getting a feel for what kinds of minimum, maximum, |
| 51:29 | 95th percentile, and 50th percentile response times are considered good or bad. |
| 51:35 | These numbers are important for helping decide whether it's worth chasing code optimization |
| 51:41 | or simply allocating more hardware. Your gut feeling, without numbers, is worthless. |
| 51:47 | "Oh, I think we need to rewrite this code in another language." Show me the numbers. |
| 51:52 | If you're not capable of generating these numbers, I know you're also incapable of rewriting anything to make it better. |
| 51:59 | I liked how this Rinha put emphasis on a backend challenge based on benchmarks and load testing. |
| 52:05 | I think every good programmer always has a benchmark tool up their sleeve |
| 52:11 | to use precisely in decision moments like the one I mentioned before: is it worth stopping to optimize? |
| 52:16 | How much improvement are we expecting at the cost of how much time to fix it? Would just scaling the machine solve it? |
| 52:23 | Measure to find out. I didn't know Gatling before, but I found it a very good tool, especially because |
| 52:28 | it generates charts that are easy to read and to show to others. There are other tools I used more in the past, like the old Apache Bench or WRK — |
| 52:36 | which I think is pronounced "work." To me those two were as essential as knowing how to use wget or curl. |
| 52:42 | Recently I played a bit with Vegeta too, which seems to be more in the Gatling category. |
| 52:47 | And there are dozens of others — open source and commercial. Don't skip out on poking around the options and testing as many as you can. |
| 52:54 | I hope you've followed so far, because now I think it's going to get a bit more complex — in the sense that I'm going to discuss higher-level concepts. |
| 53:01 | If you start to feel lost, it's the same as always — write down the terms you don't understand and look them up. |
| 53:07 | Then come back and see if you can follow the reasoning all the way through. So far it's been more of a warm-up. |
| 53:12 | I've been leaving the database aside until now so I can tackle this subject all at once right now. |
| 53:17 | I already made a video talking more about how databases work under the hood, so I recommend watching it if you haven't yet. |
| 53:24 | Having that structure in your head will be important to understand what I'm about to explain. As I said in the first Rinha video, the mistake almost everyone made in this challenge |
| 53:33 | was underestimating Postgres. If it had been MySQL it would have been the same thing — we would have assumed it was the bottleneck. |
| 53:39 | That's because in our experience on real projects, most of the time the bottleneck tends to be the database. |
| 53:46 | Why? Because in more realistic systems, with hundreds of tables, thousands of indexes, thousands |
| 53:53 | of relationships between tables, any insert or update operation usually touches |
| 53:59 | not just one row in a single table — it can trigger triggers, callbacks, to run |
| 54:04 | procedures on other tables. It can fire triggers to update multiple indexes. So no operation is "simply" just touching one row — normally there are multiple |
| 54:14 | operations happening across multiple different places. That costs time — sometimes it can take tens of milliseconds. |
| 54:21 | Same with queries, selects. In complex systems we normally have queries with multiple inner joins, outer joins, subselects, |
| 54:30 | unions. Every query, in reality, needs to hit multiple indexes, do some table scans — |
| 54:37 | so it can take time, maybe tens of milliseconds. On top of that, there's the per-connection cost. With Postgres, each connection to the database can cost in the range of 2 to 4 megabytes. 100 |
| 54:48 | connections could eat up nearly half a gigabyte of RAM. That's why we need to use connection pools to recycle connections. |
| 54:56 | When a request finishes being served, the framework returns the free connection to the pool. |
| 55:01 | That way, another request can reuse the same connection without having to allocate another 4 megabytes |
| 55:06 | every time. When we're talking about thousands of requests per second, megabytes become gigabytes |
| 55:12 | very fast if we're not careful. Remember our chart? I said a response taking 2 milliseconds is very good. |
| 55:18 | But if it takes 10 milliseconds just to insert a record into a table, won't that be bad? Yes it will. |
| 55:24 | That's why in real projects we use various strategies to spare the database as much processing as possible. |
| 55:30 | We use at least two types of strategy: one for reads, where we cache results to avoid constantly repeating the same heavy queries, and an async job queue strategy |
| 55:41 | to avoid hammering the database with too many write and modify operations. Remember: every modify operation locks one or more records in one or more |
| 55:51 | tables in the database. On top of those locks we have transactions, transaction logs so that in case |
| 55:58 | of a crash, we don't lose data or leave anything corrupted. All that protection costs processing and costs time. |
| 56:05 | Every query being run against tables that are constantly being modified means a read operation is competing with a write operation over those locks. |
| 56:13 | Meaning — if we don't have control and we receive too high a volume of writes at the same time as multiple reads, everything will slow to a halt. |
| 56:21 | That's why we need to take pressure off the database. You can create separate read-only replica servers. |
| 56:27 | That way, we can have a master database just for writes and read-only databases for |
| 56:33 | where we can send our queries. But replicas don't come free — the more replicas you add, the heavier the overhead to keep |
| 56:39 | them in sync. When we talk about caching, we're talking about parallel services like Memcache or Redis. |
| 56:44 | Even on AWS there's the ElastiCache service, which is an interface for Memcache or Redis as a service. |
| 56:50 | For queues, some people use NATS or Redis as a queue manager. They work, but without advanced features. |
| 56:57 | They don't offer many of the guarantees of a larger queue system. Just as a relational database should offer ACID protections, a real queue needs |
| 57:06 | guarantees — for example, ensuring that an element in the queue is never duplicated. Real queues are things like the legendary RabbitMQ or the more popular Apache Kafka. |
| 57:16 | AWS also has a service — SQS, which stands for Simple Queue Service. With caching, the idea is simple: first ask the cache if it has the answer. |
| 57:25 | If it doesn't, then send the query to the database, and store the result in the cache |
| 57:30 | for next time. That way, the database doesn't have to keep redundantly spending processing to answer |
| 57:36 | what it just answered. Cache is temporary memory to reduce the read workload. |
| 57:41 | With queues, the idea is a bit more complex: instead of hammering the database with modify operations, throw all those operations into a queue. |
| 57:49 | Configure a limited and controlled number of workers that sit there listening to |
| 57:55 | that queue and pull those operations off. The number of workers is the number of connections we know the database can handle without starting |
| 58:03 | to get really slow. I already explained all of this in the episode "Making Your Web App Faster" and also |
| 58:08 | in the episode where I suggest how the Ingresso.com site could improve its architecture. |
| 58:14 | And knowing all of that, naturally — since the Rinha instructions asked us to limit Docker resource usage to at most 1.5 CPUs and 3 gigs of RAM, and knowing it would run a load |
| 58:24 | test with thousands of simultaneous inserts and searches, the first instinct was to think: let's just be safe and add caching for searches, and queues and jobs for the inserts. |
| 58:34 | I did that in my first Ruby on Rails version, which you can see on my GitHub, and as we saw in the previous episode, several projects I reviewed did something similar. |
| 58:44 | Some added just a Redis cache. Some tried to add some kind of queue. But the fact is, if we stop and think coldly about exactly what this project is and what |
| 58:55 | the load looks like, you can already tell that anything more sophisticated was total |
| 59:00 | overkill. This whole little project has a single table. Just one. |
| 59:05 | No relationships, no foreign keys, no joins, nothing. Remember that spreadsheet of random data the Gatling script uses to do the inserts? |
| 59:15 | How many rows did it have? 100,000? I'd say "only" 100,000 — and over 3 minutes of load, what's the total number of requests? |
| 59:22 | Around 100,000. What's the final count of records inserted? In the range of 45 to 50,000 records. |
| 59:27 | Understand — that is absurdly small. A few thousand records is basically nothing. |
| 59:35 | Any junky computer from the 80s can handle thousands of records. A database with a single table of a measly 50,000 rows, taking no more than 100,000 |
| 59:47 | insert attempts — that's pathetically small. Real databases have hundreds of tables, most with hundreds of thousands |
| 59:56 | of rows, many with millions, tens of millions of rows. That starts to feel like "large." |
| 1:00:03 | A single table is a hello world. Therefore, as fun as it was to implement, neither a read cache nor a write job queue |
| 1:00:12 | was actually necessary. Just using the database normally was enough. Postgres is more than capable of doing simple inserts like this in an extremely |
| 1:00:22 | fast manner. In fact, it'll be in the same order of magnitude time-wise as writing to a cache store |
| 1:00:27 | like Redis — so it makes no difference in this case. Using Redis only makes a difference when writes are orders of magnitude faster because |
| 1:00:35 | Postgres is having to deal with very heavy operations on top of a truly |
| 1:00:41 | large volume of data. Get it? A lot of people still think Redis or Memcache is automatically faster than any |
| 1:00:48 | database. That's not true. An almost empty database will be just as fast as Redis. |
| 1:00:54 | Same thing if you go overboard and stuff too much into Redis — it'll get slow too. |
| 1:01:00 | Nothing in programming is automatic. If you approach it that way, everything you build will always turn out lousy. |
| 1:01:05 | "Oh, but shouldn't Redis just handle everything on its own and be faster??" |
| 1:01:11 | Obviously not, genius. But there was, in fact, one small legitimate concern. In the search-by-terms API, the Rinha asks you to search in either the name field or the |
| 1:01:21 | nickname field or the technologies field. That'd make a select with ILIKE "%blabla%" OR ILIKE "%blabla%", which is simply one of the |
| 1:01:32 | worst types of searches you can do in SQL. Because of those percent signs at the start and end, it means we have no way to use an |
| 1:01:40 | index. Indexes work for exact searches. Partial search — the only way is to do pattern matching on every row in the table. |
| 1:01:48 | Once for each field after "OR." Since we have three fields to search, that's three table scans through every row |
| 1:01:56 | in the table up to that point. That can get heavy very fast without you realizing it. |
| 1:02:01 | If at a given moment the table has 10,000 rows, we'll have to do 30,000 comparison operations. |
| 1:02:07 | And the bigger the table gets, the longer the search takes. And what's the solution? |
| 1:02:12 | I recommend watching the episode on the leaked Twitter algorithm code afterwards, where I take a tangent to explain what full-text search is, or search by embeddings |
| 1:02:22 | in vector space. I won't repeat what I explained there, but it's what's under the hood of how a Google |
| 1:02:27 | or Bing can answer any search so fast and with the most relevant results |
| 1:02:32 | right at the top of the first page of results. Every modern database has support for something similar, and it's called "full text |
| 1:02:40 | search." In Postgres specifically, there are two most well-known techniques: GIN and GiST. |
| 1:02:46 | GIN stands for Generalized Inverted Index and it's more recommended |
| 1:02:52 | for complex data structures like arrays, JSONB, hstore. It's optimized for multi-key searches and allows indexing of individual values |
| 1:03:02 | within an array, keys of a JSONB, and so on. The GIN index is built by extracting keys from the indexed values and creating a |
| 1:03:10 | posting list — an ordered list of pointers to the indexed rows. GiST stands for Generalized Search Tree. |
| 1:03:21 | GiST is a more general-purpose indexing technique and requires implementing custom search methods. |
| 1:03:27 | It can be used to index virtually any type of data in Postgres, including spatial |
| 1:03:33 | data, custom data types, and any kind of text. GiST indexes are characterized by a hierarchical node structure. |
| 1:03:41 | We can pair GiST with support for a trigram extension. I explain n-grams in the Twitter video. |
| 1:03:48 | Let's show in practice what all this mumbo jumbo actually means. To do that I'll open the code that creates the table in the database. |
| 1:03:55 | Lucas did the right thing by adding this code in Node.js. It starts by importing the "pg" library and creates a connection Pool using the |
| 1:04:03 | connection URL that comes from the DB_URL environment variable in docker compose. Then it registers an event on the pool — the "connect" event — which says: as soon as a connection is made, run |
| 1:04:12 | the following anonymous function, which will send a query to the pool. That query is a set of SQL commands. |
| 1:04:18 | Since we have two Node instances starting up at the same time, both will execute this function, so we need to write the commands so that if what we want to create already exists, |
| 1:04:27 | it just ignores it and moves on. For example, the first command says: "if it doesn't already exist, create the pg_trgm extension," which |
| 1:04:35 | is the extension that adds support for tokenizing text into trigrams. Next: "create or replace if it already exists, the function generate_searchable, which takes |
| 1:04:44 | the parameters nome, apelido and stack, all of type varchar." Remember? Varchar is variable characters — a list of characters of variable length, basically |
| 1:04:53 | a string. The function returns a value of type text, and that text will be the concatenation of the |
| 1:04:58 | fields nome, apelido and stack, all joined together. But what's that for? Bear with me — you'll understand in a second. |
| 1:05:03 | Next, if it doesn't exist, we create a table called "pessoas," which is the main table. The first field is called "id" of type UUID. |
| 1:05:11 | Optionally we can declare that if we don't pass a value explicitly, it should generate a random UUID using the gen_random_uuid() function; additionally, the constraint is that this field |
| 1:05:22 | must be unique and not null. Next, we declare the apelido field as type text, also unique and not null. |
| 1:05:29 | The nome field must be type text and not null. The nascimento field must be type Date and not null. |
| 1:05:35 | The stack field must be type JSON — one of the advantages of using Postgres, because it supports more complex data types like JSON. |
| 1:05:42 | You don't need MongoDB for that. And finally, we declare a special column called "searchable" of type text. |
| 1:05:49 | But this field is declared as a "generated" field. It's a field whose value will be automatically generated on every insert or update operation, and it will |
| 1:05:58 | use the return value of the function we declared earlier — generate_searchable. Remember what it does? |
| 1:06:03 | It returns the values of nome, apelido and stack concatenated. That's what this field will contain. But why? |
| 1:06:08 | The next SQL command is, if it doesn't exist, to create a new index called idx_pessoas_searchable, |
| 1:06:15 | on the pessoas table, using GiST. We tell GiST to index that "searchable" column using the operator class "gist_trgm_ops," |
| 1:06:24 | which is specifically designed to tokenize that field into trigrams — which are groups of 3 characters, broken up, or tokenized, from each word in the string. |
| 1:06:33 | That's what enables partial searches. The purpose of this column is to avoid having to do nome like blabla "OR" apelido like blabla |
| 1:06:43 | "OR" stack like blabla. We no longer need "OR." Just do searchable like blabla and done. |
| 1:06:50 | If any of those fields contains the blabla, it'll return it. "OR" is expensive — whenever you can avoid it, avoid it, even if it means wasting space. |
| 1:06:58 | It can be a tradeoff worth making. Searches with percent signs — wildcards — are also expensive. |
| 1:07:04 | Now let's look at the search function in Lucas's project. Further down is the "findByTerm" function. |
| 1:07:12 | Look — we define the query template with a string. Select the id, apelido, etc. fields from the pessoas table, where searchable ilike $1, limited |
| 1:07:23 | to 50 response rows. That "$1" gets replaced by the value down here, which is the search term concatenated with |
| 1:07:30 | percent signs at the start and end. Wait, but wasn't that kind of search slow? Let's dig in. |
| 1:07:35 | Every relational database has a command to explain how a query is going to be executed — |
| 1:07:40 | the search strategy. To see this, we can connect with Postgres's command-line client, psql. |
| 1:07:48 | So we can connect with "docker exec -it node-postgres-1 psql -U postgres -d postgres" |
| 1:07:57 | and done. Worth noting that I'm connecting to Postgres right after running the load test, so |
| 1:08:04 | the table already has over 46,000 rows inserted successfully. Let's start by analyzing a simple query. |
| 1:08:11 | I run "explain analyze" followed by the query "select * from pessoas where apelido = 'foo'." |
| 1:08:18 | As expected, that apelido column already has an index, so it correctly says it's going to do an "index scan" on the "pessoas_apelido_index" index. |
| 1:08:27 | Searches using an index tend to be very fast, and we can see the Execution Time here at the end saying it took only 0.037 milliseconds — 37 microseconds — super fast. |
| 1:08:39 | These other numbers up here, for example the cost — the first is the relative cost |
| 1:08:44 | to fetch the first result row (0.41) and 8.43 is the relative cost to fetch |
| 1:08:51 | the rest of the rows. Same with actual time — it's the time to fetch the first row and then the time to fetch |
| 1:08:57 | the rest of the rows. One reason it distinguishes the first result row from the rest is to give a sense of |
| 1:09:03 | the latency your application will have between firing the search and the first result |
| 1:09:09 | that comes back in streaming and could already be displayed. Since this query uses an index and the row count is absurdly small, it takes 16 |
| 1:09:18 | microseconds to pull the first row and just over 16 microseconds to pull |
| 1:09:24 | all the rest. That's genuinely fast. But now let's see how much slower the term search would be the way most |
| 1:09:31 | beginner programmers would do it. Look: `explain analyze select * from pessoas where apelido ilike '%foo%' or nome ilike |
| 1:09:41 | '%foo%' or stack::text ilike '%foo%'`. Look at how long this query takes to execute: 42 milliseconds, or 42,000 microseconds. |
| 1:09:53 | Compared to the simple query before, we're talking about a response time over a thousand times slower. |
| 1:09:58 | Yes, 42 milliseconds is still relatively fast, but considering I said |
| 1:10:03 | this table is absurdly small, and index-based queries can finish in microseconds, |
| 1:10:10 | 42 milliseconds is a long time. |
| 1:10:15 | Look at the strategy it says this query is forced to use: "seq scan" — sequential scan — literally a full table scan. |
| 1:10:24 | Imagine a for loop going from row 0 all the way to row 46,000-something and checking the fields |
| 1:10:31 | row by row. It's the worst case for a database search — checking everything with no indexes. |
| 1:10:38 | You want to avoid this kind of query like the plague, because the bigger your table gets, the slower your search gets. |
| 1:10:46 | With an index, you can double the table size and the query time barely changes. Will all of this we did — creating the searchable field concatenating nome, |
| 1:10:56 | apelido and stack, then indexing using GiST with Trigrams — actually make a difference? We can't just guess and say yes without testing. |
| 1:11:04 | So let's go: explain analyze `select * from pessoas where searchable ilike '%foo%'` |
| 1:11:10 | and look — it's faster, yes. We dropped from 42 milliseconds to just 4 milliseconds. |
| 1:11:18 | At least 10 times faster. And look at the search strategy. No more sequential scan like before. |
| 1:11:25 | Instead we have a Bitmap Heap Scan. This strategy is split in two and starts first with a Bitmap Index Scan, which will |
| 1:11:34 | use the index we built using GiST with Trigrams. In that phase we'll have a certain number of result rows. |
| 1:11:41 | Those rows will be marked in a bitmap, and in the second search phase |
| 1:11:47 | it will filter only the marked rows, using that recheck condition shown in |
| 1:11:52 | the strategy. Meaning it still has a row scan phase, but not on the entire table — only on the rows |
| 1:12:00 | returned in the first stage of the index search. In this case, the index alone doesn't give the full answer, which is why Bitmap Heap Scan exists. |
| 1:12:09 | It's slower than an Index Scan, like in the first example we tested, but it's much faster than a full sequential scan. |
| 1:12:17 | So yes — all of that GiST setup was worth it. And it's a solid tip for any project. In larger projects where we'd be using things like Redis caching, we could also |
| 1:12:27 | delegate full-text search to another external service. I always recommend the tried-and-true ElasticSearch. |
| 1:12:34 | Elastic is essentially a NoSQL database specialized in full-text search. |
| 1:12:40 | DevOps and monitoring folks also typically integrate ElasticSearch with services like |
| 1:12:45 | Kibana and Logstash for log searching, for example. Elastic is for when your project grows and features like GiST are no longer holding up |
| 1:12:54 | under the processing load on the database. Never over-engineer in the first days of a project. |
| 1:13:00 | Only specialize into external services when you have the load to justify it. |
| 1:13:05 | The more external services, the harder it'll be to maintain everything. It's a trade-off. |
| 1:13:11 | Everything has its time, and that time is not day one. Anyway — are you starting to get it? A simple SQL operation on an untuned Postgres running on my machine |
| 1:13:21 | is on the order of microseconds. Way under 1 millisecond. Databases were built to be fast. |
| 1:13:27 | Watch my episodes on Trees and Databases. It's way more than just appending a new line to the end of a file. |
| 1:13:35 | Another technique I had set aside to cover in this episode is SQL Bulk Insert. |
| 1:13:41 | Many of the winning projects — like Vinicius's in Rust or André and Albert's in C# — did |
| 1:13:46 | the trick of accumulating inserts either in an external queue or in memory, with a background job |
| 1:13:52 | picking up groups of inserts and sending them all at once to the database. But honestly there's not much to comment on here. |
| 1:13:57 | The concept is pretty simple. Instead of every request having to connect to the database, fire an insert, wait for the result, |
| 1:14:04 | and then respond, we can have it respond without knowing if the write will succeed or not. And keep accumulating. |
| 1:14:10 | After accumulating a reasonable amount — say 100 inserts — then send them all at once inside a single transaction. |
| 1:14:16 | Just by not having to create a new transaction for every individual insert — plus sending everything over |
| 1:14:21 | the network in one shot — saves a lot of time. But as I've said a thousand times in this episode, 100,000 simple insert attempts is a light |
| 1:14:29 | load. I think for almost none of the participant projects did they actually need to use this technique — |
| 1:14:35 | at most, not using it would have made little difference. Bulk insert and also upserts are important techniques. |
| 1:14:43 | Data warehouse, business intelligence, data science people — anyone who needs to clean |
| 1:14:48 | data and load large volumes all at once knows they need bulk insert, otherwise |
| 1:14:53 | they're wasting time. Each database has its own syntax quirks, so it's better for you to just Google |
| 1:14:58 | "postgres bulk insert" or "postgres batch insert" — the first links explain it |
| 1:15:04 | clearly. I'm not going to repeat what's already easy to find in simple tutorials. More important is getting back to docker compose and talking about Postgres configuration. |
| 1:15:13 | Each Rinha participant did this part slightly differently, but generally everyone was focused on changing this one parameter: max_connections. |
| 1:15:22 | Most guessed somewhere around 450 connections. Remember a connection costs about 2 megabytes? |
| 1:15:28 | If we actually use all 450 max connections, the Postgres container could |
| 1:15:33 | eat nearly 900 megabytes. That's way too much, considering we only have 3 gigabytes total for everything. |
| 1:15:40 | So there's no point in cranking this number up. There are a few different ways to change this setting. |
| 1:15:45 | The simplest is exactly like this — changing the Postgres command line and adding the parameter directly. |
| 1:15:51 | Another way is like we did with nginx — creating a postgres.conf file to map |
| 1:15:57 | as a volume inside the container. And another way is like we created the table and indexes: directly from the application, when it first connects, |
| 1:16:04 | it sends SQL commands, and one of those commands would be to increase |
| 1:16:10 | the maximum connections. But the more important question is: how many connections do we actually need to support for the type |
| 1:16:17 | of load our Gatling test is going to run? Is 450 really enough? |
| 1:16:22 | Or would less work? And if less, how much less? If we configure the application's connection pool to keep 450 connections open permanently, we'd be |
| 1:16:31 | wasting a lot of RAM. If we allocate too few connections, the first requests will spend extra time waiting for |
| 1:16:38 | new connections to open. There's a balance here — and that's exactly what load tests are for: validating whether our assumptions |
| 1:16:45 | actually hold up in practice. To make our lives easier, we already saw that we can connect via the psql |
| 1:16:51 | command-line client to run queries directly in the database. But we can add some more visual tools to make things easier |
| 1:16:58 | both for you and for me to demonstrate here. The one I saved for last is pgAdmin — a veteran we've been using for over |
| 1:17:05 | a decade, since before Docker was even invented. It's very simple — just add this service to your docker compose. |
| 1:17:12 | Email and password don't matter — it's just because it'll spin up a web admin interface. The important thing is to declare this port 5050, because otherwise it'll try to come up on port 80, and |
| 1:17:22 | since we're in host network mode, it'll try to bind to the real port 80 of localhost, and it won't have permission without being root, so it'll fail at boot. |
| 1:17:30 | Change it to any port above 1000, like 5050. Now we can run "docker compose up" and wait a few seconds. |
| 1:17:39 | Let's fast-forward — and done. In the browser on the side we can load localhost:5050 and look |
| 1:17:44 | at that, beautiful. Just enter the same email and password we configured and we're in. Go poke around this interface later and read the docs on their site to learn what's what. |
| 1:17:54 | But I'll leave an important warning: despite being useful, never, ever expose a service |
| 1:18:00 | like this to the public internet. It'd be monumentally stupid to leave this unprotected — if someone gets in, they'll have |
| 1:18:09 | access to your production database. Nothing from any production database should ever appear on the public internet. |
| 1:18:16 | That's asking to get hacked. Spin it up temporarily in production for a specific task, but take it back down |
| 1:18:22 | as fast as possible. Use it in staging, in dev environments, on the company's private network, |
| 1:18:30 | inside a DMZ — but never on the public internet. No more, no less — you've been warned. |
| 1:18:36 | Anyway, I had already registered the Postgres we were using in the tests, but let me |
| 1:18:41 | register it again so you can see how it works. Give it any name, address is localhost, user is postgres, password is the same one |
| 1:18:48 | you set in docker compose, and done — connected. But so what — what can we do with this? |
| 1:18:54 | Let's go to the other terminal and run the Gatling load test again. I'm speeding through the edit here, but look at this — we can see in real time |
| 1:19:03 | the monitoring of things like transactions per second. I'll jump ahead to the middle of the test. |
| 1:19:08 | Notice how the total transactions are in blue, but we have roughly fifty-fifty between transactions that committed OK and transactions that rolled back from failure. |
| 1:19:18 | This is easy to explain. Remember how the load test makes a total of about 100,000 requests, but only about 46,000 |
| 1:19:25 | actually get written? The others error out because fields like apelido and nome have unique constraints — |
| 1:19:32 | no duplicates allowed. When we try to insert a duplicate, it rejects it and rolls back. That's why it's roughly fifty-fifty. |
| 1:19:38 | Below we can see charts to get a sense of the volume of data coming in and going out. Each set of fields — the row itself — is a tuple. |
| 1:19:45 | We have tuples coming in (inserts), tuples going out (query results), an I/O chart that I'm not sure what the unit of measure is — whether it's read/write operations or byte count or something like that. |
| 1:19:55 | (I/O metrics continued.) More important is this first chart at the top. |
| 1:20:01 | Follow the blue line — that's the total number of connections to the database at this moment. Green is connections where operations are happening, like our inserts or selects. |
| 1:20:10 | Red is idle open connections doing nothing. Notice how the connection count is roughly stable, even knowing that in the load test |
| 1:20:18 | we're gradually increasing the user count per second, ramping up to 600 users per second. |
| 1:20:24 | And that's the importance of a good connection pool. The same connection gets reused, recycled, across multiple requests. |
| 1:20:32 | That's why you don't need that many. Only right at the very end — the very end — when we hit the 95th percentile territory, |
| 1:20:40 | maybe 75th percentile, that 5 to 15% that takes over 1 second to respond, that's when |
| 1:20:45 | the number of simultaneously open connections spikes dramatically. In that final stretch you get the impression that the request in Node is so slow it doesn't have time |
| 1:20:53 | to return the connection to the pool, so it keeps having to open more and more connections. Those last requests taking over 5 seconds are completely out of |
| 1:21:02 | the realm of normal. If in a real product we were regularly seeing response times in that range, either |
| 1:21:08 | something needs optimization, or you urgently need to scale up the infrastructure. |
| 1:21:14 | We shouldn't configure max database connections, or the connection pool size, based on that final spike. |
| 1:21:20 | Contrary to what many might expect, during almost the entire load test, it never exceeded 40 simultaneous active connections — it was around 20. |
| 1:21:29 | Not 450, not 300, not even 100. Only at the very end — but by then it was already garbage time. |
| 1:21:35 | So if anyone guessed that max database connections would be the bottleneck, they guessed wrong too. |
| 1:21:40 | And why is it under 40 at a time? If we go back to docker compose, in the application configuration, look at the |
| 1:21:48 | environment variables section. There's this DB_POOL we pass to the "pg" library's pool used by Node. |
| 1:21:53 | In this case it's set to 35. We have two instances, so the max would be 70. |
| 1:21:58 | Even so, for most of the test, neither pool ever gets saturated. Only at the end. |
| 1:22:04 | At that point it doesn't much matter whether we'd set it higher or not. The infrastructure just can't handle that final spike. |
| 1:22:09 | So what's the right number for the connection pool? Every blog post and Stack Overflow answer you find will have some magic formula trying to |
| 1:22:18 | guess. But there's no shortcut — to really know, you have to run a load test, monitor |
| 1:22:23 | via pgAdmin, and through trial and error, run the test for different |
| 1:22:29 | pool size values. That's the only reliable way to know whether you're setting a number that's too tight or |
| 1:22:35 | too loose. Don't trust anything except tests you ran yourself. |
| 1:22:41 | With all that said, many of you watching — including several of the participants, and myself — were certain that Postgres would be the problem. |
| 1:22:50 | But now that we've measured and have real data, we know for sure we were wrong. The techniques themselves work, but the application and the test load were much smaller than |
| 1:23:01 | we imagined. We overestimated the problem and over-engineered. |
| 1:23:06 | Classic programmer over-engineering. The lesson is simple: write the simplest code that solves the given problem, take |
| 1:23:13 | measurements, and only then decide where you actually need to optimize — and keep measuring |
| 1:23:20 | to see whether the optimization actually helped, or made things worse. And that brings us to the final topics left on my list. |
| 1:23:29 | I said I'd talk about Crystal, about Kotlin, and then about threads and fibers. But I want to keep it brief. |
| 1:23:36 | During the 16-languages video I wanted to show that yes, with virtually any language, |
| 1:23:41 | you could hit the maximum of the load test. Some with a bit more work than others, but everyone was capable of passing or coming |
| 1:23:49 | very close. Does that mean all languages are equally valid? Or equally performant? |
| 1:23:55 | Obviously not. Let's use some common sense here. Clearly, all languages compiled to native binaries have a performance edge — |
| 1:24:02 | Rust and Go primarily, followed by C++, Java and its derivatives like Kotlin or Scala, |
| 1:24:09 | C#, Crystal, Zig, even Bun. That's a clear performance category A. |
| 1:24:15 | Interpreted languages — PHP, JavaScript and its derivatives like TypeScript, Ruby, Python — |
| 1:24:22 | all belong to the same performance category B. You simply cannot reach the same level of raw performance as category A. |
| 1:24:30 | It is possible to scale web applications to similar numbers, at the cost of a little |
| 1:24:36 | more hardware. That's because much of a web application depends on I/O, and I/O will be slower regardless of |
| 1:24:43 | whether the application is compiled or interpreted. So yes, the infrastructure for category B languages needs to be a bit bigger than category |
| 1:24:51 | A. But not orders of magnitude bigger — that's the point. On the other side of the equation is how easy it is to find good developers, or train |
| 1:25:00 | new ones — and that's where category B languages far outpace |
| 1:25:06 | category A. Ask any Rust enthusiast and they'll tell you I'm wrong, that Rust is totally easy, they learned it no problem. |
| 1:25:12 | But I hate to say it: I believe you, but your profile is different from the general population. |
| 1:25:18 | People are not all the same. Those who find becoming fluent in Rust "easy" are a tiny fraction of the population. |
| 1:25:25 | The vast majority have neither the desire nor the ability to become fluent in low-level |
| 1:25:31 | languages that demand a much more detailed understanding of everything underneath — |
| 1:25:36 | the network, the operating system, the hardware. And this isn't unique to Rust. |
| 1:25:42 | Back in the 80s and 90s we always had Assembly, C, C++, Objective-C. |
| 1:25:47 | But which languages became popular? In the 80s it was things like COBOL or dBase. |
| 1:25:53 | In the 90s it was things like Visual Basic or Delphi. Delphi even compiled to native, but what attracted people was the tooling, the IDE, |
| 1:26:02 | the Interbase database that came bundled. Nobody used Delphi to write OS drivers or anything like that. |
| 1:26:09 | Now it's the same story. There's always been a category of people who found it easy to program in C, C++, even Assembly — |
| 1:26:16 | and now Rust, Go. To really take advantage of those languages, you need to be the type of person |
| 1:26:22 | who enjoys learning intimately how the hardware works underneath, the entire operating system underneath. |
| 1:26:28 | For category B, someone who doesn't care and doesn't even know the difference between RAM and an SSD can still produce something. |
| 1:26:37 | The learning curve is noticeably lower, which is why more people end up in those category B languages. |
| 1:26:43 | Duh — before some idiot comments: obviously there are advanced programmers in category B too. |
| 1:26:48 | But a mediocre developer won't survive in category A. So yes, it's possible to build an entire web startup with just Rust or Go, but good luck |
| 1:26:57 | maintaining a team of dozens or hundreds of people that scales with the company's growth. |
| 1:27:03 | It won't keep up. That's why every major tech company uses a mix of technologies. |
| 1:27:09 | There's no giant company that can use a single language for everything. And that's without even counting the ecosystem. |
| 1:27:16 | Productivity doesn't come from the language alone. It comes from the libraries, products, services, documentation, courses, books, events, and |
| 1:27:23 | everything else that orbits around the language. It's much harder to compare languages and just say "oh, it's compiled" or "oh, it's |
| 1:27:30 | interpreted." That's the least important part of the comparison. There's also a cultural dimension. |
| 1:27:35 | Let me give a dumb example. Anyone from the JavaScript world is used to going to the terminal and typing something like "npx |
| 1:27:42 | create-react-app my-app." Anyone from Ruby on Rails is used to going to the terminal and typing "rails new my-app." |
| 1:27:49 | Anyone from Python is used to running "django-admin startproject my-app" |
| 1:27:55 | and so on. How does someone from Java start a new project? They go to start.spring.io, fill out a freaking form, which generates the project and gives |
| 1:28:04 | you a zip file to download. Then you manually unzip it, and that's the beginning of your project. |
| 1:28:10 | Almost in 2024, and to me that smells like 2004. Other languages like C# have command-line options with dotnet core, but how does |
| 1:28:19 | most of that community actually prefer to do it? Yeah — open Visual Studio Enterprise and click New Project and start filling things in. |
| 1:28:27 | To JavaScript, Ruby, Python, PHP people, that feels incredibly outdated. |
| 1:28:33 | But to Java or C# people, having a CLI to generate things feels like unnecessary fluff when |
| 1:28:39 | the website or IDE already does everything. And that's one of those cases that depends entirely on your perspective. |
| 1:28:44 | Neither side will ever agree on this. But that kind of disagreement in perspective is exactly what guarantees we'll never have |
| 1:28:52 | a single language and a single ecosystem as the standard for everything. I hate having to use an IDE. |
| 1:28:58 | Rather than use IntelliJ or Visual Studio, I'd switch to any language where I can be productive purely from the command line, using NeoVim. |
| 1:29:06 | Non-negotiable for me. I already made a video just explaining the differences between compiled and interpreted languages |
| 1:29:13 | and going deeper on all of this — recommend you watch it afterward. But regarding Crystal and Kotlin specifically, which I analyzed in the Rinha. |
| 1:29:20 | Both are newer languages with small communities. In Kotlin's case, it was created by a large company — JetBrains — and later backed |
| 1:29:28 | by Google itself. And even so, adoption isn't as large as I'd like to see at this point. |
| 1:29:34 | Again, people already comfortable with just Java don't see much value in migrating to Kotlin. In the end the bytecode is compatible — why bother switching? |
| 1:29:41 | For those of us used to more modern languages, Kotlin is an excellent option |
| 1:29:46 | for when we're forced to do something on the Java platform. Worth keeping in your arsenal. |
| 1:29:52 | But there will always be that divide. I feel like Kotlin is more appealing to people who aren't from Java than to those |
| 1:29:59 | already experienced in Java. Crystal is much smaller and more niche. Even so I think it's an incredibly competent, stable language, with a minimal but functional ecosystem. |
| 1:30:09 | I find Ruby's syntax far superior to Go's. Cancel me, but it's true. |
| 1:30:15 | But obviously Ruby is in a much lower performance category than Go. Crystal, though, is genuinely Ruby syntax with Go-level performance — the same order of magnitude. |
| 1:30:25 | Yes, Go is still better optimized, and being a more primitive language, it also compiles |
| 1:30:30 | faster. But for things like web microservices, I find Crystal and frameworks like Lucky far |
| 1:30:36 | more enjoyable to use than their Go or Rust equivalents. If productivity matters and avoiding fatigue matters too, Crystal can be a solid alternative — |
| 1:30:45 | especially for small teams. The learning curve is lower. Worth having in your arsenal. And I reinforce this after having tried languages like Nim, which has existed for many more |
| 1:30:54 | years, but is still awful to use — poorly documented, few usable libraries, |
| 1:31:00 | and lots of bugs. It feels like an experimental language that I wouldn't recommend for production use under any circumstances. |
| 1:31:06 | You could spend all day just talking about various languages, and I already covered them extensively in |
| 1:31:11 | the original Rinha episode, so go watch that afterward. But getting back to the topic of database connections, I just wanted to make a comment about the difference |
| 1:31:18 | between compiled languages like Rust and interpreted ones like JavaScript. Or more specifically, languages that can scale with native threads versus languages that |
| 1:31:26 | can't, due to a global interpreter lock — which all category B languages have. |
| 1:31:32 | I explain this in the compiled vs. interpreted languages episode. But in our database use case it typically means the following: beyond being more performant |
| 1:31:40 | because they're compiled, languages like Rust or even Java have the advantage of needing fewer simultaneous connections. |
| 1:31:48 | A Java connection pool is thread-safe, meaning it can be used by multiple native threads in parallel. |
| 1:31:55 | In a language like JavaScript or Ruby — or any interpreted language — each native thread needs its own connection pool. |
| 1:32:01 | In short, it works roughly like this: in Java you could configure the connection pool to have a max of 100 connections, and that's what the entire application will have: 100 |
| 1:32:09 | connections. In JavaScript, it depends. If you run with a single process, you'll spin up one connection pool — set the max |
| 1:32:16 | to 100, and that's what you get: up to 100. Node.js's reactor is single-threaded. Every async call runs on the same native thread and accesses a single connection pool. |
| 1:32:27 | In Node.js cluster mode, it can spin up multiple forked Node processes. |
| 1:32:32 | Each one with a separate connection pool. So if you're spinning up 5 forks in the cluster, you should configure each pool to have a max of 20 connections |
| 1:32:40 | if you want to maintain the same total. 5 times 20 equals a max of 100 — get it? That's why you can't spin up infinite forks in the cluster — because each fork gets fewer |
| 1:32:50 | and fewer connections available in its pool. I'm skipping over a lot of details here. Every language will have its own quirks in how it implements threads, coroutines, |
| 1:32:59 | and pools. You need to understand how non-blocking I/O works and how your language of choice handles |
| 1:33:06 | it. Homework assignment, in case you haven't studied that yet. The tip is to start by watching my old episode on concurrency and parallelism to build |
| 1:33:15 | that foundation. But the point was just to make you aware that there are big differences in how you configure |
| 1:33:20 | things like connection pools in Java versus Node.js, for example. The configuration of things like connection pools will be different. |
| 1:33:28 | The best way to learn the difference is by monitoring with something like pgAdmin and creating |
| 1:33:34 | load test scenarios with Gatling or Vegeta, to see in practice which numbers work |
| 1:33:39 | and which don't. When you're a junior, you do everything blind, by gut feel, assuming everything will work out. |
| 1:33:47 | But as you gain experience and grow more senior, you start to understand that flying by the seat of your pants is the fastest road to bad software. |
| 1:33:55 | The software always seems to do things you didn't anticipate. To gain control over that, there's only one path: learning about benchmarking, profiling, |
| 1:34:03 | load testing, monitoring, measurement, statistics — and finally starting to see and correctly |
| 1:34:10 | interpret software behavior under different levels of load and stress. |
| 1:34:16 | And with that, I think I've managed to cover all the topics I had left pending. |
| 1:34:21 | I didn't want to close out the year with loose ends. With that, I can finally declare this whole Rinha topic closed. |
| 1:34:26 | Let's start the new year with new subjects. In January 2024 I'll be on vacation spending three weeks in Japan, so I don't think |
| 1:34:34 | there'll be a video in January. I don't know yet whether I'll be back posting before or after Carnival, so it might be |
| 1:34:39 | that I only return somewhere between February and March. Take advantage and binge the channel. I'll probably be posting about the trip on Instagram, so follow me there. |
| 1:34:48 | If you have questions, drop them in the comments below. If you liked the video, leave a thumbs up, subscribe to the channel, and don't forget to share the video with your friends. |
| 1:34:56 | See you in 2024 — Merry Christmas and Happy New Year! |
| 1:35:29 | See you later! |
